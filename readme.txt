gcc -g first.c -o first.exe




what preprocessor does
gcc -E first.c



what assemble code
gcc -S first.c



int testing[] = {10, 20, 30, 40};

int x = sizeof(testing)/sizeof(testing[0]);
int s;
int x = sizeof(testing)/sizeof(testing[0]);

scanf("%d", &s);
    printf("entered value is %d: ", s);
added now again test
